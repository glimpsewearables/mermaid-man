import React from 'react';

const Footer = (props) => {
    console.log(props);
    return (
        <div className="Footer" style={props.backgroundStyle}>
            
        </div>
    );
};

export default Footer;